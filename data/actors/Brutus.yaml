# Heavy Melee Build
name: Brutus
level: 3
armor: 0
vitality: 0
weapons: [HeavyMaul]
tactics:
  swing_attack # 0.92
  #if_prepared_for_foe(swing_attack,if_foe_adjacent(swing_attack, prepare))
  #if_multiple_attackers(if_foe_defending(approach, swing_attack), if_prepared_for_foe(recover, swing_attack)) #0.93
  #if_multiple_attackers(swing_attack, if_prepared_for_foe(swing_attack,prepare))
